[⬅ Back to Hardware Architecture](../docs/hardware_architecture.md)

---
# LED Signal Tower – Hardware Design

## Overview

This document describes the hardware design of the PoE-powered LED signal tower controller.

The system is built around an **ESP32-C3** and is designed to integrate a simple LED signal tower into existing automation environments such as:

* Home Assistant
* IoT platforms
* monitoring systems
* industrial control environments

The primary design goal is **reliable system integration with minimal hardware complexity**.

Universal support for arbitrary industrial signal towers is **not the main objective** of the current prototype.

---

# System Goals

The prototype hardware aims to achieve the following:

* PoE powered network device
* ESP32-C3 controller
* three independent LED outputs
* simple hardware architecture
* minimal component count
* robust operation
* easy integration into existing systems

---

# Hardware Architecture

The system is based on a **12 V system rail**.

```
PoE
 │
 └── PoE Splitter / DC-DC
        │
        ├── 12 V → LED tower
        │
        └── Step-Down → ESP32-C3 supply
```

The LED segments are switched directly on the **12 V rail** using transistor outputs.

---

# Early Design Concepts

Several power architectures were evaluated during development.

## Step-Up Converter per Channel (Rejected)

Initial idea:

```
5 V
 ├── Boost → LED RED
 ├── Boost → LED YELLOW
 └── Boost → LED GREEN
```

### Motivation

* compatibility with unknown LED modules
* independent voltage generation per channel

### Disadvantages

* unnecessary DC/DC conversion stages
* larger BOM
* more complex PCB layout
* lower efficiency
* boost modules used in the prototype had **no accessible enable pin**

Due to these drawbacks this design was abandoned.

---

# Final Architecture

The final design switches the LED segments directly on the **12 V rail**.

Advantages:

* simpler circuit design
* fewer components
* higher efficiency
* easier debugging
* better reliability

---

# LED Tower Characteristics

The prototype uses a **generic no-name LED signal tower**.

Important observation:

The LED modules operate correctly **independent of polarity**.

This means the tower can be wired either with:

* common ground
* or common +12 V

For the final design the following configuration was chosen:

```
Common +12 V
Low-side switching
```

This enables a very simple transistor driver stage.

---

# Output Stage Concept

Each LED segment is controlled using a **low-side NPN transistor switch**.

Simplified circuit:

```
             +12 V
               │
               │
          LED Segment
               │
               │
             Collector
                │
             NPN Transistor
                │
             Emitter
                │
               GND

ESP GPIO ── 2.2 kΩ ── Base
                     │
                   100 kΩ
                     │
                    GND
```

---

# Output Channels

The system provides three independent outputs:

| Channel    | Function       |
| ---------- | -------------- |
| LED_RED    | red status     |
| LED_YELLOW | warning status |
| LED_GREEN  | ready / OK     |

Each channel is controlled by an ESP32-C3 GPIO pin.

---

# Power Supply

The system expects a **12 V supply rail**, typically generated by a PoE splitter.

```
PoE
 │
 └── PoE Splitter
        │
        ├── 12 V → LED tower
        │
        └── Step-Down → ESP32
```

Recommended additions:

* 100 µF capacitor on 12 V rail
* 100 nF decoupling capacitor near ESP32

---

# Design Philosophy

The hardware design prioritizes:

* simplicity
* robustness
* minimal component count
* easy maintainability

Given the extremely small current consumption of the LED tower, a simple transistor driver stage provides **more than sufficient electrical margin**.

## Prototype Implementation

The hardware design was validated using several physical prototype revisions.

| Revision | Description |
|--------|-------------|
| Rev.A | Initial perfboard prototype layout |
| Rev.B | Layout adjusted for 28-row perfboard |

Detailed documentation of each prototype can be found here:

[Prototype Rev.A](led_tower_prototype_rev_a.md)
[Prototype Rev.B](led_tower_prototype_rev_b.md)